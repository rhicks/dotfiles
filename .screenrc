hardstatus alwayslastline
hardstatus string '%{= kG}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{r}(%{W}%n*%f %t%?(%u)%?%{r})%{w}%?%+Lw%?%?%= %{g}][%{B} %Y-%m-%d %{W}%c %{g}'
# Default screens
# screen -t shell1        0
# screen -t shell2        1
# screen -t server        2
#
#do not use xterm's alternative window buffer it break scrollback
termcapinfo xterm ti@:te@

